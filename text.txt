def setUp(self):
        """Set up a DatasetDiffChecker instance for testing."""
        self.mail_to = "keeliput"
        self.env = "prod"
        self.dataset_diff_checker = DatasetDiffChecker(
            mail_to=self.mail_to,
            env=self.env,
            pull_datasets_from_fdc=MagicMock()
        )
