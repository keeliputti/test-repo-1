for final_configs in config_processor.process_configs(package):
        provider_config = final_configs.get(config_processor.prov.provider_id)
        package_config = final_configs.get(config_processor.pack.package_id)
        dataset_config = final_configs.get(config_processor.dataset.dataset_id)
