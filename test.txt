def try_import(module_name, object_name, warning_message):
    try:
        module = __import__(module_name, fromlist=[object_name])
        return getattr(module, object_name)
    except ImportError:
        warnings.warn(warning_message)
        return None


discuss and evaluate different options for providing our users (or dataset onboarders) with a model to configure their settings.
